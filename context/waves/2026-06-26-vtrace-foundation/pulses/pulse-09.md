# Pulse 09: INTERFACES

Settled. Authored `docs/vtrace/INTERFACES.md`: inventory IF-001..006 (corpus file incl.
scale/market enum, sources registry, rubric record, tier/SLA record, `tarmac-network` API,
`tarmac` CLI with `--scale`), per-interface details, compatibility/change-control rules, and
open questions (incl. IFQ-004 scale nesting). Fixed point; no change required. Next: CODE_RIGOR.
